# VISR : VIsualization System for RBPS

visr or (read as *viser*) is a tool intended to visualize the simulatios made with rbps.

visr is made with raylib for the main rendering of objects and uses ImGui for the user interfaces.

 